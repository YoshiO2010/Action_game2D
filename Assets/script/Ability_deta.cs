using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(menuName = "Ability_deta")]

public class Ability_deta : ScriptableObject
{
    // Start is called before the first frame update
    public string id;
    public string name;
    public Sprite icon;
    public string class_name;
}
